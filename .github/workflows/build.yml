name: Build APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Kodu çek
        uses: actions/checkout@v4

      - name: Java 17 kur
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Android SDK kur
        uses: android-actions/setup-android@v3

      - name: Gradle wrapper jar indir
        run: |
          mkdir -p gradle/wrapper
          curl -sL "https://github.com/gradle/gradle/raw/v8.4.0/gradle/wrapper/gradle-wrapper.jar" \
            -o gradle/wrapper/gradle-wrapper.jar

      - name: Linphone SDK indir
        run: |
          mkdir -p app/libs
          curl -sL "https://linphone.org/maven_repository/org/linphone/linphone-sdk-android/5.3.0/linphone-sdk-android-5.3.0.aar" \
            -o app/libs/linphone-sdk-android.aar
          ls -lh app/libs/

      - name: gradlew izni
        run: chmod +x gradlew

      - name: APK derle
        run: ./gradlew assembleDebug --no-daemon --stacktrace

      - name: APK yükle
        uses: actions/upload-artifact@v4
        with:
          name: SipDoor-debug-apk
          path: app/build/outputs/apk/debug/app-debug.apk
          retention-days: 30
